defmodule CampusChat.InsertQueryTest do
  use CampusChat.DataCase

  alias CampusChat.Message

  test "insert message" do
    str = "There will be tests here someday"
    assert str == "Ahahaha, No"
  end
end
